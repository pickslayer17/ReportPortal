# Frontend's single source for the backend URL (dev). Vite exposes VITE_* to the app.
# At deploy, set VITE_API_URL in the real environment before `vite build` — no code change.
VITE_API_URL=http://localhost:5002
